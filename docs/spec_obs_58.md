# 观测向量 58 维（obs_version=3）

| 索引 | 维数 | 含义 |
|------|------|------|
| 0 | 1 | 己方开火阶段 ∈ [-1,1]：冷却中 `−剩余/90`；就绪后 `经过/90`（再计 1.5s 到 +1） |
| 1-6 | 6 | 敌方：局部 dx, dy, dθ/π, 进退 m∈{-1,0,1}, 转向 r∈{-1,0,1}, 同上开火阶段 |
| 7-46 | 40 | **10** 子弹槽 × (局部 dx, dy, φ/π, valid)；槽 0–4 己弹、5–9 敌弹；存活期内 id 固定占槽 |
| 47-54 | 8 | 车体 8 向墙雷达：距离 / (房间较长边的一半)；未命中 ≈ 2；0=正前，逆时针 |
| 55-57 | 3 | A*：局部下一步角/π, 归一化路径长, reachable |

维度公式：`1 + 6 + 4×bullet_slots + wall_radar_rays + 3`，见 `tank_sim.observation.spec`。

相对早期 99 维（7×7 墙栅格）的变化：墙感知改为 8 向射线测距，单帧 99→58。

相对 v2 的变化：

- 弹槽 8→10（容量对齐 5+5），威胁排序改为**固定身份槽**
- 子弹 `(vx,vy)` → 局部航向 `φ/π`（定速）
- 敌车 `(lvx,lvy,ω)` → 离散 `m,r`（定速进退/转向）
- 己方 / 敌方开火阶段双段编码：冷却 [-1,0]，就绪后再计 1.5s → (0,1]

历史堆叠（MLP 训练）：瞄准课程为 **选择性稀疏堆叠** `frame_stack=3`、`frame_stride=3`：敌方 / 墙雷达 / A* 取 **t-6,t-3,t**；**己方开火阶段与全部子弹槽仅当前帧** → **92** 维；若 `stack_action_mean=true`，再拼接同 lag 的确定性动作均值（3 维×3）→ **101** 维。`frame_stride=1` 时为连续采样。基线 `ppo_mlp_sb3.yaml` 仍为 16 连续整帧堆叠 → 928 维。

瞄准课程空场亦为 58 维；世界边界为外框墙，雷达测墙 AABB。旧无雷达变体见 [`spec_obs_50.md`](spec_obs_50.md)。

LSTM 阶段：不堆叠，单帧 58 维。
